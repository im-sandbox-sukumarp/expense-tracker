"""
Test Package for Expense Tracker Application.

This package contains all test modules for the Expense Tracker application.
Tests are organized into separate modules based on the functionality being
tested:

Modules:
    conftest: Pytest fixtures and configuration for all tests.
    test_app_setup: Tests for application configuration and setup.
    test_edge_cases: Tests for edge cases and error handling.
    test_models: Tests for database models.
    test_routes: Tests for HTTP routes and API endpoints.

Example:
    Run all tests using pytest::

        $ pytest tests/

    Run tests with coverage::

        $ pytest tests/ --cov=app

    Run a specific test module::

        $ pytest tests/test_routes.py
"""
